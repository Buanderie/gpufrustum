function bench_createfigure(xdata1, ydata1, zdata1)
%CREATEFIGURE(XDATA1,YDATA1,ZDATA1)
%  XDATA1:  surface xdata
%  YDATA1:  surface ydata
%  ZDATA1:  surface zdata

%  Auto-generated by MATLAB on 05-Jun-2009 19:47:54

% Create figure
figure1 = figure;

% Create axes
axes1 = axes('Parent',figure1,'ZScale','log','ZMinorTick','on',...
    'ZMinorGrid','on',...
    'YScale','log',...
    'YMinorTick','on',...
    'YMinorGrid','on',...
    'XScale','log',...
    'XMinorTick','on',...
    'XMinorGrid','on',...
    'CLim',[0 0.2]);
view([8.5 32]);
grid('on');
hold('all');

% Create surf
surf(xdata1,ydata1,zdata1,'Parent',axes1,'DisplayName','a, x, z');

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% contour3 currently does not support code generation, enter 'doc contour3' for correct input syntax

% contour3(...);

% Create xlabel
xlabel({'Frustums',''},'HorizontalAlignment','right');

% Create ylabel
ylabel('Bounding Volumes','HorizontalAlignment','left');

% Create zlabel
zlabel({'Duration (s)',''});

% Create colorbar
colorbar('peer',axes1,[0.8714 0.1114 0.01852 0.815],'CLim',[0.001 1]);

